<%= form_for(@rule_set) do |f| %>
  <% if @rule_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rule_set.errors.count, "error") %> prohibited this rule_set from being saved:</h2>

      <ul>
      <% @rule_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :routing_order %>
    <%= f.number_field :routing_order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
